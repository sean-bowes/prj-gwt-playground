package com.koisoftware.monet.client.common.type;

import java.io.Serializable;

public enum SchemaType implements Serializable {
    INTEGER, LONG, FLOAT, DOUBLE, DECIMAL, DATE, TIME, STRING, BOOLEAN;
}
